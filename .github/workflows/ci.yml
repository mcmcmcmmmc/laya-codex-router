name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install evaluation test dependencies
        run: python -m pip install --disable-pip-version-check scipy
      - name: Byte-compile all Python sources
        run: python -m compileall -q server poc evals
      - name: Unit tests
        run: python -m unittest discover -s server -p "test_*.py" -v
      - name: Evaluation tooling tests
        run: python -m unittest discover -s evals -p "test_*.py" -v
